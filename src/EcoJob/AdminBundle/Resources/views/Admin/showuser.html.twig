    <div class="team1 clearfix">
        <figure><img src="{{ vich_uploader_asset(user.curriculum, 'imageFile') }}" alt="{{ user.curriculum.nom }}" class="img-responsive" height="200px" width="200px" /></figure>
        <div class="caption">
            <div class="txt1">{{user.curriculum.etatCivil.nom}} {{user.curriculum.etatCivil.prenom}}</div>
            <div class="txt2">{{user.curriculum.etatCivil.telephone}}</div>
            <div class="txt3">{{user.curriculum.anneeExp}}</div>
        </div>
    </div>